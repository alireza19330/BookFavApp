Project Technology Stack:
Spring, Spring Boot, Spring MVC, MySQL/h2, JPA, Thymeleaf, CSS, HTML, MAVEN
______________________________________
Using maven to install:
mvn clean install

using "java -jar demo-0.0.1-SNAPSHOT.jar" to run the project standalone.
Use "application.properties" besides the project to edit the properties like datasource name and so on.
At startup 100 numberbooks are added to the database. There is no pre-defined user, user can register throgh web page.